import React from 'react'
import RenderPages from './RenderPages'
import { render } from '@testing-library/react';

describe('RenderPages', () => {
    test('renders RenderPages component', () => {
        // render(<RenderPages routeList={[]} notFoundComponent={""} />)
        render(<div>RenderPages component the test will be in the future</div>)
    })
})
