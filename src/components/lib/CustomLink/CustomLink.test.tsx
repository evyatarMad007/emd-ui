import React from 'react'
import CustomLink from './CustomLink'
import { render } from '@testing-library/react';

describe('CustomLink', () => {
    test('renders CustomLink component', () => {
        // render(<CustomLink href='/' linkType='subHeader' children={"kjkjkj"}></CustomLink>)
        render(<div>CustomLink will be testing a later...</div>)
    })
})
